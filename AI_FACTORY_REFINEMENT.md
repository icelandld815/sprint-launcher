Task: Live PR validation 3
Requested action: Create a refinement PR artifact

Refinement loop:
1. Capture the current intent and repository context.
2. Produce deterministic next actions that a future model-backed runner can refine.
3. Persist an auditable execution artifact set for later PR or issue follow-up.

Planned next actions:
- Inspect task source `dashboard` and normalize execution context.
- Translate requested action into deterministic steps: Create a refinement PR artifact
- Prepare artifacts that capture the current execution plan and proposed refinements.
- Attach repository context for `icelandld815/sprint-launcher` and prepare GitHub follow-up.
